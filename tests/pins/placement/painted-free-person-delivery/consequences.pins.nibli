# SPDX-License-Identifier: MIT OR Apache-2.0
:expect-pins 16

# The candidate changes the baseline FALSE consequence.
? dwell(Free_NotSevere_NoFamily_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Free_NotSevere_NoFamily_Home).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Free_NotSevere_Family_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Free_NotSevere_Family_Home).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Free_Severe_NoFamily_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Free_Severe_NoFamily_Home).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Free_Severe_Family_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Free_Severe_Family_Home).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_NotSevere_NoFamily_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_NotSevere_NoFamily_Home).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_NotSevere_Family_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_NotSevere_Family_Home).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_Severe_NoFamily_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_Severe_NoFamily_Home).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_Severe_Family_NoHome).
# => TRUE

# The candidate changes the baseline FALSE consequence.
? dwell(Registered_Severe_Family_Home).
# => TRUE
