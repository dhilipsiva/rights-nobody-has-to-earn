# SPDX-License-Identifier: MIT OR Apache-2.0
:expect-pins 6

? complete(JNoAuthority0Record, JusticeChargingProcedure, JNoAuthority0Case).
# => FALSE
? complete(JNoAuthority1Record, JusticeChargingProcedure, JNoAuthority1Case).
# => FALSE
? complete(JNoAuthority2Record, JusticeChargingProcedure, JNoAuthority2Case).
# => FALSE
? complete(JNoAuthority3Record, JusticeChargingProcedure, JNoAuthority3Case).
# => FALSE
? complete(JNoAuthority4Record, JusticeChargingProcedure, JNoAuthority4Case).
# => FALSE
? complete(JNoAuthority5Record, JusticeChargingProcedure, JNoAuthority5Case).
# => FALSE
