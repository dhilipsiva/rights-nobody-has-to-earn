SPDX-License-Identifier: CC-BY-4.0

Active TODO: Meet the five-minute complete-verification target.
Implementation remains unfinished; do not start another constitutional family.

Scope: speed up the runner and its companion engine without changing the live
constitution, pins, refusals, scoped retractions, stateful sequences, trusted
shell preconditions or complete contradiction checks. No persisted verdicts,
input hashes, receipts, administrative gates or Book 2 operations.

The companion changes are committed and pushed as fdd9de1 and 3f64f0b.
The cumulative patch against fa91a5f is retained in
new-book-plans/nibli-performance-candidate.patch. It contains no temporary
profiling hooks or unrelated companion changes; its reverse applicability
was checked against the actual checkout.

The first integration's normal verifier passed all 12,860 pins across 4,190
cases in 526.66 seconds, with complete contradiction checks, no findings and
nine existing defects reproduced. The completed-cone extension's scratch
binary passed the same inventory in 457.07 seconds (7m37.07s). Both used four
workers and prebuilt release binaries. This session started no other
development build/test during either measurement; other machine work was
not controlled. The original measured baseline was 1,552.00 seconds.

The rebuilt normal ./verify.sh passed against 3f64f0b in 448.90 seconds
(7m28.90s): all 12,860 pins across 4,190 cases, complete contradiction checks,
no findings and nine existing defects reproduced. Four workers and a prebuilt
release binary; this session started no other development build/test during
the measurement. Other machine work was not controlled. README and the
performance notes record this result. Do not close the TODO unless a full
run passes under five minutes.

Latest engine integration checks:
- Native and WebAssembly CI passed through just ci-all, including all 12
  oracle/differential tests (96.53 seconds).
- All 637 reasoner tests pass, including completed-tuple reuse, outside
  inserts, shape refusals, budget boundaries and invalid-prior safeguards.
- The extension's broad mutation sweep had nine caught, two unbuildable and
  two missed. A new defensive-helper regression caught both missed guards
  by assertion failures in a focused rerun with the same test packages.
  Combined: 11 caught, two unbuildable, none unresolved; no timeouts credited.
- The normal runner's development suite passed 24 tests; two manual probes
  are intentionally ignored.

Nibli origin uses SSH without a usable key. Push/fetch via
https://github.com/dhilipsiva/nibli.git without changing the user's remote.
The companion origin/main tracking ref was refreshed after the push.

Scratch workspace: /tmp/rights-nibli-perf.llSbQz. Its release book-runner binary
is the measured completed-cone candidate. Scratch source also contains
temporary test-only loading timers and an eager-unary-guard experiment, which
are NOT integrated. Do not copy this source wholesale into the companion.
The borrowed-join experiment passed its tests but showed no useful overall
probe improvement; it was removed and saved as borrowed-joins.patch there.

Next performance lead, not an implemented or measured gain: both
collect_group_event_candidates and collect_entailment_candidates in
nibli-reason/src/kb.rs copy all_typed_domain_members() into a Vec, although
the accessor already returns &[GroundTerm] and candidate selection only reads
it. The granular loading profile spent about 0.10 seconds proving 264 witness
activations in the arrest case. Try borrowing the cached slice, preserving all
ordinary anchor selection, fallback candidates, binding and provenance checks.
Remove the separate eager-unary experiment before comparing this change.
Model construction and actual scoped-control retractions also remain costly.
