SPDX-License-Identifier: CC-BY-4.0

IN FLIGHT — 2026-09-15

Author instruction for this run: work the TODO backlog one item at a time —
implement, verify, commit, push, then take the next item — without asking
questions. This supersedes the 2026-09-14 stop condition that was recorded here
for the ecological item.

VERIFIER PERFORMANCE ITEM — PROFILED, NOT CLOSED

The complete inventory passes: all 77,902 pins across 13,566 cases with complete
contradiction checks and no findings in 824.25 seconds (13m44.25s), four workers,
release binary prebuilt, nine known-defect expectations still reproducing. Peak
RSS 21,626,012 KiB, user CPU 3,174.52 s, 394% utilisation, no swaps. That misses
the five-minute target and the tracker bullet stays open.

What this run added is the measurement the bullet asked for, not a speedup. The
phase table, the case shapes and each lead's measured size are in
new-book-plans/nibli-performance-candidate.md. In short: about 3,143 cumulative
worker-seconds against the roughly 1,200 the target needs; queries 1,652 and
fixture loading 624 of them; contradiction scans below one per cent.

Three leads were measured and priced down rather than pursued: preparation
already builds each of the 127 variant bases exactly once, batch-wide fixture
hoisting is worth nothing because the intersection over each of the 389 batches
is empty by construction, and caching compiled fixture statements is worth about
60 worker-seconds because compilation is only a tenth of fixture time.

The largest sized lead is about 15% and is engine-internal: the first query in a
fresh snapshot costs 37-48 ms whatever it asks, because the knowledge base's
Clone drops both the saturation and the witness closure, so every case re-derives
the same 256 witness activations. Carrying them across a snapshot needs a domain
dependency cone in nibli-reason and the companion's own soundness checks.

The temporary engine instrumentation used to find that was reverted; the
companion checkout is back at 979fe8b with a clean tree. The runner's manual
probe keeps the two ecology cases, the two degenerate cases and the
RIGHTS_PROFILE_CONE query measurement, so the finding reproduces.

The stale README claim that the verifier meets the five-minute target — measured
on the 4,190-case inventory that preceded the ecological family — is corrected in
the same change.
